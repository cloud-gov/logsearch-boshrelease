{
  "index_templates": ["<%= p('elasticsearch_config.index_prefix') %>*"],
  "prioirity": 100,
  "template": {
    "settings": {
      "number_of_shards": 5,
      "number_of_replicas" : 1
    }
  }
}
